version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - DB_SERVER=db
      - DB_NAME=HotelBooking
      - DB_USER=sa
      - DB_PASSWORD=LuxeStay_Admin2026!
    depends_on:
      - db

  db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    ports:
      - "1433:1433"
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=LuxeStay_Admin2026!